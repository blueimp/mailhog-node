version: '2'

services:
  mailhog:
    image: blueimp/mailhog

  test:
    image: node:6-slim
    entrypoint: node test.js
    user: nobody
    working_dir: /srv/www/
    volumes:
      - ".:/srv/www"
    environment:
      - MAILHOG_HOST=http://mailhog:8025
