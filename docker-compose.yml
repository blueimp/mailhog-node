version: '2'

services:
  mailhog:
    image: blueimp/mailhog

  test:
    image: node:6-slim
    entrypoint: npm run test
    working_dir: /app
    volumes:
      - ".:/app"
    environment:
      - MAILHOG_HOST=http://mailhog:8025
